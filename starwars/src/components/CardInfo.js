import React from 'react';
import './StarWars.css';

const CardInfo = props => {
    const {name,birth_year, eye_color, hair_color, height, gender} = props.character;
    return (

}

export default CardInfo;