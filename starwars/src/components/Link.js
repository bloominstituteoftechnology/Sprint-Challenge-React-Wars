import React from "react";

const BigLink = (props) => {
console.log("Why not")

  return (
    <div>
      <p>Hi?</p>
      <a href={props.link}>Film!</a>
    </div>
  );
};

export default BigLink;
