//right here we will be having a static h1 for a title displaying stuff about chracters
import React from "react";

const cardTitle = () => {
//components must always return something

    return (
        <h1>Below IS STARWARS STUFF</h1>
    );

};

export default cardTitle; //You always have to export your components so other parts can use them.
