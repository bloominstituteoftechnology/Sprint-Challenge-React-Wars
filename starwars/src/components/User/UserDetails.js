import React from 'react';
import './user.css';

const UserDetails = (props) => {

    // let characters = props.characters.slice(0, 1);
    // let luke = characters[0]['name'];

console.log();
return (
        <div>
      <p>   {props.swChar.name} </p>
        </div>
  
);
};

export default UserDetails;
