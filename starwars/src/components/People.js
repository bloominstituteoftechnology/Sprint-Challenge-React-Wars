import React from 'react';
import styled from 'styled-components';

const People = (props) => {
    return (
        
            // {props.name}
            // {props.height}
            // {props.gender}
            // {props.mass}
            // {props.hair_color}
            // {props.eye_color}
        
        
    )
}

export default People