import React from 'react';
import Tab from './Tab';
// Display a list of tabs that hold the character's names.

const Tabs = (props) => {
    return (
        <div>Tabs.js
            <Tab />
        </div>
    );
};

export default Tabs;