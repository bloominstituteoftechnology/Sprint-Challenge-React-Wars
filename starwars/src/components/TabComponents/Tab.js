import React from 'react';
// Displays a clickable tab to pull up more information

const Tab = (props) => {
    // Props:
    // - name: character name
    return (
        <div>{props.name}</div>
    );
};

export default Tab;