import React from 'react';
import App from './App'

const Character =  props =>{
  
    return (
        <div> 


            <h1>  {props.stuff.name}</h1>
            </div>
    );
};

 
 export default Character;