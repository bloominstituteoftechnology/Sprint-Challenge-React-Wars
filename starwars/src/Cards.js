// Use Birthyear, created, edited, eye_color, gender, hair_color, height, mass, name, skin_color

import React from 'react';

const Cards = props => {
    return (
        <div> {props.name} </div>
    )
}

export default Cards;
